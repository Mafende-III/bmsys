# ----- Database -----
# Postgres on the same VPS, accessed via localhost
DATABASE_URL="postgresql://bms_user:CHANGE_ME@localhost:5432/bms?schema=public"

# ----- Auth.js -----
# Generate with: openssl rand -base64 32
AUTH_SECRET="CHANGE_ME_TO_A_LONG_RANDOM_STRING"
AUTH_URL="https://bmsys.streamlinexperts.rw"
AUTH_TRUST_HOST=true

# ----- Initial seed values (used by `pnpm prisma:seed`) -----
OWNER_NAME="Owner"
OWNER_PHONE="+250788000000"
OWNER_PIN="1234"

# ----- App -----
NEXT_PUBLIC_APP_URL="https://bmsys.streamlinexperts.rw"
NODE_ENV="production"
